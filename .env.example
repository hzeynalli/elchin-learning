# Copy to .env (never commit). Front-end reads only the two PUBLIC values at build/run time.
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=
PUBLIC_WORKER_URL=
# Worker secrets — set with `wrangler secret put NAME`, listed here for reference only
ANTHROPIC_API_KEY=
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=
SUPABASE_SERVICE_KEY=
SUPABASE_JWT_SECRET=
